<Window x:Class="WindowsHelperSuite.Overlay.Views.OverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        ShowActivated="False"
        Title="Writer"
        Width="Auto"
        Height="Auto"
        SizeToContent="WidthAndHeight"
        MaxWidth="900"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True"
        FontFamily="Segoe UI">

    <Window.Resources>
        <Style x:Key="PillButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <ContentPresenter/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <!-- Drop shadow wrapper -->
    <Border Padding="12">
        <Border.Effect>
            <DropShadowEffect Color="#000000" BlurRadius="32" ShadowDepth="8" Direction="270" Opacity="0.55"/>
        </Border.Effect>

        <!-- Glass backdrop panel -->
        <Border x:Name="BackdropPanel"
                CornerRadius="18"
                BorderThickness="1">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                    <GradientStop Color="#E0101828" Offset="0"/>
                    <GradientStop Color="#E8081420" Offset="1"/>
                </LinearGradientBrush>
            </Border.Background>
            <Border.BorderBrush>
                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                    <GradientStop Color="#55FFFFFF" Offset="0"/>
                    <GradientStop Color="#18FFFFFF" Offset="1"/>
                </LinearGradientBrush>
            </Border.BorderBrush>

            <StackPanel Orientation="Vertical" Margin="0">
                <!-- Header bar -->
                <Border Padding="16,10,16,8" CornerRadius="18,18,0,0">
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                            <GradientStop Color="#22FFFFFF" Offset="0"/>
                            <GradientStop Color="#08FFFFFF" Offset="1"/>
                        </LinearGradientBrush>
                    </Border.Background>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <!-- Animated dot -->
                            <Ellipse x:Name="StatusDot" Width="7" Height="7" Fill="#4ADE80" Margin="0,0,8,0" VerticalAlignment="Center">
                                <Ellipse.Effect>
                                    <DropShadowEffect Color="#4ADE80" BlurRadius="6" ShadowDepth="0" Opacity="0.9"/>
                                </Ellipse.Effect>
                            </Ellipse>
                            <TextBlock Text="WRITER" FontSize="11" FontWeight="Bold"
                                       Foreground="#AAFFFFFF" VerticalAlignment="Center"/>
                        </StackPanel>
                        <TextBlock Grid.Column="1" x:Name="HintLabel"
                                   Text="Esc to close"
                                   FontSize="11" Foreground="#55FFFFFF"
                                   VerticalAlignment="Center"/>
                    </Grid>
                </Border>

                <!-- Separator line -->
                <Rectangle Height="1" Margin="12,0,12,0">
                    <Rectangle.Fill>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                            <GradientStop Color="#00FFFFFF" Offset="0"/>
                            <GradientStop Color="#33FFFFFF" Offset="0.5"/>
                            <GradientStop Color="#00FFFFFF" Offset="1"/>
                        </LinearGradientBrush>
                    </Rectangle.Fill>
                </Rectangle>

                <!-- Suggestion pills (horizontal) -->
                <WrapPanel x:Name="SuggestionsContainer"
                           Orientation="Horizontal"
                           Margin="10,8,10,4"
                           MaxWidth="860"/>

                <!-- Separator between word pills and AI sentence (only shown when sentence visible) -->
                <Rectangle x:Name="SentenceSeparator"
                           Height="1" Margin="12,0,12,4"
                           Visibility="Collapsed">
                    <Rectangle.Fill>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                            <GradientStop Color="#00FFFFFF" Offset="0"/>
                            <GradientStop Color="#22FFFFFF" Offset="0.5"/>
                            <GradientStop Color="#00FFFFFF" Offset="1"/>
                        </LinearGradientBrush>
                    </Rectangle.Fill>
                </Rectangle>

                <!-- AI sentence prediction row (shown when AI returns a sentence) -->
                <StackPanel x:Name="SentenceContainer"
                            Orientation="Vertical"
                            Margin="10,0,10,8"
                            Visibility="Collapsed"/>
            </StackPanel>
        </Border>
    </Border>
</Window>
