<Window x:Class="WindowsHelperSuite.App.ModeToastWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        SizeToContent="WidthAndHeight"
        ShowActivated="False"
        Focusable="False">
    <Border Background="#1A1B23"
            BorderBrush="#4ADE80"
            BorderThickness="2"
            CornerRadius="12"
            Padding="20,14"
            Margin="12">
        <TextBlock x:Name="MessageText"
                   FontSize="16"
                   FontWeight="SemiBold"
                   FontFamily="Segoe UI"
                   Foreground="#F0F0F5"
                   TextWrapping="Wrap"
                   MaxWidth="360"
                   TextAlignment="Center"/>
    </Border>
</Window>
